# TrakNor CMMS Frontend - Production Environment Variables
# Copy to .env.production and configure for your cloud deployment

# =============================================================================
# API Configuration
# =============================================================================

# API Base URL Pattern for Multi-Tenant
# Option 1: Subdomain-based (recommended)
#   - Set VITE_API_URL_PATTERN=https://{tenant}.api.traknor.com
#   - Frontend at: https://umc.app.traknor.com
#   - Backend at: https://umc.api.traknor.com/api
#
# Option 2: Path-based (single domain)
#   - Set VITE_API_URL=https://api.traknor.com/api
#   - Tenant is sent via header or determined by JWT
#
# Option 3: Fixed tenant (single-tenant deployment)
#   - Set VITE_API_URL=https://api.mycompany.com/api
#   - VITE_DEFAULT_TENANT=mycompany

# For subdomain-based multi-tenant (replace {tenant} dynamically)
VITE_API_URL_PATTERN=https://{tenant}.api.traknor.com/api

# Fallback/default API URL (used when tenant cannot be determined)
VITE_API_URL=https://api.traknor.com/api

# Default tenant slug (used when not detectable from hostname)
VITE_DEFAULT_TENANT=umc

# =============================================================================
# App Configuration
# =============================================================================
VITE_APP_NAME=TrakNor CMMS
VITE_APP_VERSION=1.0.0

# =============================================================================
# Feature Flags
# =============================================================================
VITE_ENABLE_DEMO_MODE=false
VITE_ENABLE_ANALYTICS=true

# =============================================================================
# Support Contact
# =============================================================================
VITE_SUPPORT_EMAIL=suporte@traknor.com

# =============================================================================
# Cloud Provider Specific (optional)
# =============================================================================
# AWS
# VITE_AWS_REGION=us-east-1
# VITE_CLOUDFRONT_URL=https://dxxxxxx.cloudfront.net

# Azure
# VITE_AZURE_CDN_URL=https://traknor.azureedge.net

# Google Cloud
# VITE_GCP_CDN_URL=https://traknor.storage.googleapis.com
